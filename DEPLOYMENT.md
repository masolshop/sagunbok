# 🚀 사근복 AI 스튜디오 배포 가이드

## 📋 배포 완료 정보

### ✅ 배포 상태
- **상태**: 배포 완료 및 실행 중 ✅
- **URL**: http://3.34.186.174
- **GitHub**: https://github.com/masolshop/sagunbok
- **서버**: AWS Lightsail (서울 리전)
- **스펙**: 2GB RAM, 2 vCPUs, 60GB SSD
- **비용**: $12/월 (약 16,000원)

---

## 🎯 주요 기능

### 1. 절세 계산기 시스템
- **기업 절세 계산기**: 사근복 전환을 통한 법인세 절감 시뮬레이션
- **CEO 절세 계산기**: 증여세/상속세 절감 및 주식 평가 시뮬레이션
- **직원 절세 계산기**: 근로자 4대보험료 및 소득세 절감 계산
- **네트 급여 계산기**: 실수령액 역산 계산

### 2. AI 기능
- **AI 챗봇**: Google Gemini API 연동 컨설팅 챗봇
- **리스크 진단**: AI 기반 기업 복지 리스크 분석
- **2025년 정책**: 최신 근로복지기금 지원사업 데이터 반영

### 3. 관리자 기능
- **대시보드**: 상담 데이터 관리 및 분석
- **데이터 저장**: 로컬 스토리지 기반 상담 이력 관리

---

## 🔑 사용자 API 키 설정

### 사용자가 해야 할 일:
1. 웹사이트 접속: http://3.34.186.174
2. 좌측 메뉴에서 **"🔑 API 키 설정"** 클릭
3. Google Gemini API 키 발급:
   - https://aistudio.google.com/app/apikey 방문
   - "Create API Key" 클릭
   - API 키 복사
4. 사근복 앱에서 API 키 입력 및 저장
5. AI 챗봇 및 진단 기능 사용 가능!

### 보안 특징:
- ✅ API 키는 사용자 브라우저에만 저장 (서버 전송 안 함)
- ✅ 로컬 스토리지 또는 세션 스토리지 선택 가능
- ✅ 공용 PC에서는 세션 저장 권장

---

## 🛠️ 기술 스택

### Frontend
- **Framework**: React 19.2.3 + TypeScript
- **Build Tool**: Vite 6.2.0
- **AI SDK**: @google/genai 1.35.0
- **스타일링**: Tailwind CSS (inline)

### Backend/Infrastructure
- **Server**: Nginx 1.18.0
- **OS**: Ubuntu 22.04 LTS
- **Cloud**: AWS Lightsail
- **배포**: Static SPA (Single Page Application)

---

## 📦 로컬 개발 환경 설정

```bash
# 저장소 클론
git clone https://github.com/masolshop/sagunbok.git
cd sagunbok

# 의존성 설치
npm install

# 개발 서버 실행 (localhost:5173)
npm run dev

# 프로덕션 빌드
npm run build

# 빌드 결과 미리보기
npm run preview
```

---

## 🔄 재배포 방법

### 코드 수정 후 재배포:

```bash
# 1. 로컬에서 빌드
npm run build

# 2. 빌드 파일 압축
cd dist && tar -czf ../dist.tar.gz . && cd ..

# 3. AWS로 전송 (SSH 키 필요)
scp -i lightsail-key.pem dist.tar.gz ubuntu@3.34.186.174:/var/www/sagunbok/

# 4. AWS에서 압축 해제
ssh -i lightsail-key.pem ubuntu@3.34.186.174
cd /var/www/sagunbok
tar -xzf dist.tar.gz
rm dist.tar.gz

# 5. Nginx 재시작
sudo systemctl reload nginx
```

### 간편 배포 스크립트 (deploy.sh 생성):

```bash
#!/bin/bash
# deploy.sh

echo "🚀 Building..."
npm run build

echo "📦 Compressing..."
cd dist && tar -czf ../dist.tar.gz . && cd ..

echo "📤 Uploading to AWS..."
scp -i lightsail-key.pem dist.tar.gz ubuntu@3.34.186.174:/tmp/

echo "🔄 Deploying on AWS..."
ssh -i lightsail-key.pem ubuntu@3.34.186.174 << 'EOF'
  cd /var/www/sagunbok
  tar -xzf /tmp/dist.tar.gz
  rm /tmp/dist.tar.gz
  sudo systemctl reload nginx
EOF

echo "✅ Deployment complete!"
echo "🌐 Visit: http://3.34.186.174"
```

```bash
# 실행 권한 부여
chmod +x deploy.sh

# 배포 실행
./deploy.sh
```

---

## 🔐 AWS Lightsail 방화벽 설정

### Lightsail 콘솔에서 네트워킹 탭:

| 애플리케이션 | 프로토콜 | 포트 범위 | 소스 |
|------------|---------|----------|------|
| SSH | TCP | 22 | 내 IP만 |
| HTTP | TCP | 80 | 0.0.0.0/0 |
| HTTPS | TCP | 443 | 0.0.0.0/0 |

---

## 🌐 SSL (HTTPS) 설정 (선택사항)

### 도메인 연결 후:

```bash
# Certbot 설치
sudo apt install -y certbot python3-certbot-nginx

# SSL 인증서 발급
sudo certbot --nginx -d yourdomain.com

# 자동 갱신 설정
sudo certbot renew --dry-run
```

---

## 📊 다음 단계 (향후 개발)

### 우선순위 높음:
1. ✅ **API 키 설정 기능** - 완료!
2. ⏳ **Google Sheets 연동** - 회원가입/로그인 시스템
3. ⏳ **사용자 역할 관리** - 사근복 전문가 / 기업 담당자
4. ⏳ **데이터 저장소** - Firebase/Supabase 백엔드 연동

### 우선순위 중간:
5. ⏳ **OpenAI GPT 지원** - Gemini 외 추가 AI 모델
6. ⏳ **PDF 보고서 생성** - 계산 결과 다운로드
7. ⏳ **커스텀 GPT 연동** - 사근복 전문 GPT 통합
8. ⏳ **SSL/HTTPS 적용** - 도메인 연결 시

### 추가 개선사항:
- 모바일 반응형 최적화
- 다크모드 지원
- 계산 결과 공유 기능
- 이메일 알림 기능

---

## 🆘 트러블슈팅

### 1. 사이트 접속 안 됨
```bash
# Nginx 상태 확인
ssh -i lightsail-key.pem ubuntu@3.34.186.174
sudo systemctl status nginx

# Nginx 재시작
sudo systemctl restart nginx
```

### 2. AI 챗봇 작동 안 함
- API 키 설정 확인 (좌측 메뉴 "🔑 API 키 설정")
- 브라우저 콘솔(F12) 에러 확인
- Gemini API 할당량 확인

### 3. 계산 결과 이상
- 브라우저 캐시 삭제 (Ctrl+Shift+R)
- 입력값 재확인
- 로컬 스토리지 초기화

---

## 📞 연락처 및 지원

- **GitHub Issues**: https://github.com/masolshop/sagunbok/issues
- **배포 URL**: http://3.34.186.174
- **AWS 콘솔**: https://lightsail.aws.amazon.com/

---

## 📝 버전 정보

- **버전**: v1.0.0 (MVP)
- **배포일**: 2026-01-20
- **최근 업데이트**: 사용자 API 키 입력 기능 추가

---

## 🎉 성공적인 배포를 축하합니다!

이제 사용자들이 본인의 API 키로 AI 기능을 사용할 수 있습니다.
보안이 강화되고, 서버 비용 부담도 줄어들었습니다! 🚀
